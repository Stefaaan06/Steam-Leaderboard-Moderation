//Cache for entries
// Key format in these objects: `${leaderboardid}-${steamid}`
const moderationEdits = {};
const removedEntries = {};

module.exports = {
    moderationEdits,
    removedEntries
};
